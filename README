# Escalonamento por Loteria

Escalonador por loteria desenvolvido para o trabalho de Sistemas Operacionais. 

Foram modificados os arquivos proc.c, proc.h e todos os demais que se utilizam da função fork(). Adicionado o arquivo teste.c

## Começando a usar

Possuir o qemu instalado
```sh
sudo apt install qemu
```

Para executar
```sh
make qemu
```

Rodar o arquivo de teste
```sh
teste
```

Pode ser necessário dar permissões aos arquivos. Para isso, forneça as devidas permissões dentro da pasta:
```sh
sudo chmod 777 *
```
